{
  "name": "@ukehoot-net/cdk",
  "version": "0.0.0",
  "private": true,
  "description": "AWS CDK infrastructure for ukehoot.net (domain, hosting, DNS).",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist cdk.out",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:update": "vitest run -u --passWithNoTests",
    "test:watch": "vitest",
    "cdk": "cdk",
    "synth": "cdk synth",
    "deploy": "cdk deploy --all",
    "deploy:stack": "cdk deploy",
    "diff": "cdk diff",
    "check:redirects": "node scripts/check-redirects.mjs",
    "smoke": "node scripts/smoke-test.mjs",
    "indexnow:ping": "node scripts/indexnow-ping.mjs"
  },
  "nx": {
    "implicitDependencies": [
      "@ukehoot-net/site"
    ],
    "targets": {
      "synth": {
        "dependsOn": [
          "build",
          "@ukehoot-net/site:build"
        ],
        "inputs": [
          "default",
          "^default",
          {
            "env": "ALERT_EMAIL"
          },
          {
            "env": "CDK_DEFAULT_ACCOUNT"
          },
          {
            "env": "CDK_DEFAULT_REGION"
          }
        ],
        "outputs": [
          "{projectRoot}/cdk.out"
        ],
        "cache": true
      },
      "deploy": {
        "dependsOn": [
          "build",
          "@ukehoot-net/site:build"
        ],
        "cache": false
      },
      "deploy:stack": {
        "dependsOn": [
          "build"
        ],
        "cache": false
      },
      "diff": {
        "dependsOn": [
          "build",
          "@ukehoot-net/site:build"
        ],
        "cache": false
      }
    }
  },
  "dependencies": {
    "@composurecdk/acm": "^0.9.0",
    "@composurecdk/budgets": "^0.9.0",
    "@composurecdk/cloudformation": "^0.9.0",
    "@composurecdk/cloudfront": "^0.9.0",
    "@composurecdk/cloudwatch": "^0.9.0",
    "@composurecdk/core": "^0.9.0",
    "@composurecdk/iam": "^0.9.0",
    "@composurecdk/route53": "^0.9.0",
    "@composurecdk/s3": "^0.9.0",
    "@composurecdk/sns": "^0.9.0",
    "aws-cdk-lib": "^2.262.1",
    "constructs": "^10.7.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "aws-cdk": "^2.1133.0",
    "vitest": "^4.1.10"
  }
}
